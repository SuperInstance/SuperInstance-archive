apiVersion: apps/v1
kind: Deployment
metadata:
  name: ai-tools
  namespace: activelog
  labels:
    app: activelog-ai-tools
    component: ai
    version: v1
spec:
  replicas: 2
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 1
  selector:
    matchLabels:
      app: activelog-ai-tools
  template:
    metadata:
      labels:
        app: activelog-ai-tools
        component: ai
        version: v1
      annotations:
        prometheus.io/scrape: 'true'
        prometheus.io/port: '9090'
        prometheus.io/path: /metrics
        sidecar.istio.io/inject: 'true'
    spec:
      serviceAccountName: activelog-ai-tools
      containers:
      - name: ai-tools
        image: activelog/ai-tools:latest
        imagePullPolicy: IfNotPresent
        ports:
        - name: http
          containerPort: 8031
          protocol: TCP
        - name: metrics
          containerPort: 9090
          protocol: TCP
        - name: health
          containerPort: 8080
          protocol: TCP
        env:
        - name: PORT
          value: '8031'
        - name: NODE_ENV
          valueFrom:
            configMapKeyRef:
              name: activelog-config
              key: NODE_ENV
        - name: LOG_LEVEL
          valueFrom:
            configMapKeyRef:
              name: activelog-config
              key: LOG_LEVEL
        resources:
          requests:
            memory: 1Gi
            cpu: 800m
          limits:
            memory: 1Gi
            cpu: 1200m
        livenessProbe:
          httpGet:
            path: /health
            port: health
          initialDelaySeconds: 30
          periodSeconds: 10
          timeoutSeconds: 5
          failureThreshold: 3
        readinessProbe:
          httpGet:
            path: /ready
            port: health
          initialDelaySeconds: 5
          periodSeconds: 5
          timeoutSeconds: 3
          failureThreshold: 3
        securityContext:
          allowPrivilegeEscalation: false
          runAsNonRoot: true
          runAsUser: 1000
          capabilities:
            drop:
            - ALL
      terminationGracePeriodSeconds: 30
      dnsPolicy: ClusterFirst
      restartPolicy: Always
---
apiVersion: v1
kind: Service
metadata:
  name: ai-tools
  namespace: activelog
  labels:
    app: activelog-ai-tools
    component: ai
  annotations:
    prometheus.io/scrape: 'true'
    prometheus.io/port: '9090'
spec:
  type: ClusterIP
  ports:
  - name: http
    port: 8031
    targetPort: http
    protocol: TCP
  - name: metrics
    port: 9090
    targetPort: metrics
    protocol: TCP
  selector:
    app: activelog-ai-tools
---
apiVersion: v1
kind: ServiceAccount
metadata:
  name: activelog-ai-tools
  namespace: activelog
  labels:
    app: activelog-ai-tools
---
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: ai-tools-hpa
  namespace: activelog
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: ai-tools
  minReplicas: 2
  maxReplicas: 8
  metrics:
  - type: Resource
    resource:
      name: cpu
      target:
        type: Utilization
        averageUtilization: 70
  - type: Resource
    resource:
      name: memory
      target:
        type: Utilization
        averageUtilization: 80
  behavior:
    scaleDown:
      stabilizationWindowSeconds: 300
      policies:
      - type: Percent
        value: 50
        periodSeconds: 60
    scaleUp:
      stabilizationWindowSeconds: 60
      policies:
      - type: Percent
        value: 100
        periodSeconds: 15
      - type: Pods
        value: 2
        periodSeconds: 60
---
apiVersion: policy/v1
kind: PodDisruptionBudget
metadata:
  name: ai-tools-pdb
  namespace: activelog
spec:
  minAvailable: 1
  selector:
    matchLabels:
      app: activelog-ai-tools
