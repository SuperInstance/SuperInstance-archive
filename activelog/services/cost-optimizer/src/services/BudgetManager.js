const EventEmitter = require('events');

class BudgetManager extends EventEmitter {
  constructor() {
    super();
    this.budgets = new Map();
    this.alerts = new Map();
    this.spending = new Map();
    this.notifications = new Map();
    this.alertTypes = {
      WARNING: { threshold: 0.8, severity: 'warning' },
      CRITICAL: { threshold: 0.9, severity: 'critical' },
      EXCEEDED: { threshold: 1.0, severity: 'exceeded' }
    };
    this.checkInterval = 300000; // 5 minutes
    this.budgetCheckTimer = null;
    
    this.startBudgetMonitoring();
  }

  createBudget(config) {
    const budgetId = `budget_${Date.now()}_${Math.random().toString(36).substr(2, 9)}`;
    
    const budget = {
      id: budgetId,
      name: config.name,
      amount: config.amount,
      currency: config.currency || 'USD',
      period: config.period || 'monthly', // monthly, quarterly, yearly
      scope: config.scope, // project, user, department, global
      scopeIds: config.scopeIds || [], // specific IDs within scope
      startDate: config.startDate || new Date().toISOString(),
      endDate: config.endDate || this.calculateEndDate(config.startDate, config.period),
      alerts: config.alerts || [\n        { threshold: 0.5, type: 'warning', enabled: true },\n        { threshold: 0.8, type: 'warning', enabled: true },\n        { threshold: 0.9, type: 'critical', enabled: true },\n        { threshold: 1.0, type: 'exceeded', enabled: true }\n      ],\n      actions: config.actions || [], // automated actions when thresholds are hit\n      tags: config.tags || {},\n      createdAt: new Date().toISOString(),\n      createdBy: config.createdBy,\n      status: 'active',\n      currentSpending: 0,\n      projectedSpending: 0,\n      utilizationPercentage: 0\n    };\n\n    this.budgets.set(budgetId, budget);\n    this.initializeBudgetTracking(budget);\n    \n    return budget;\n  }\n\n  calculateEndDate(startDate, period) {\n    const start = new Date(startDate || Date.now());\n    const end = new Date(start);\n    \n    switch (period) {\n      case 'monthly':\n        end.setMonth(end.getMonth() + 1);\n        break;\n      case 'quarterly':\n        end.setMonth(end.getMonth() + 3);\n        break;\n      case 'yearly':\n        end.setFullYear(end.getFullYear() + 1);\n        break;\n      case 'weekly':\n        end.setDate(end.getDate() + 7);\n        break;\n      default:\n        end.setMonth(end.getMonth() + 1);\n    }\n    \n    return end.toISOString();\n  }\n\n  initializeBudgetTracking(budget) {\n    this.spending.set(budget.id, {\n      budgetId: budget.id,\n      totalSpending: 0,\n      dailySpending: [],\n      lastUpdated: new Date().toISOString(),\n      breakdown: {\n        compute: 0,\n        storage: 0,\n        network: 0,\n        other: 0\n      }\n    });\n  }\n\n  updateBudgetSpending(budgetId, costData) {\n    const budget = this.budgets.get(budgetId);\n    const spending = this.spending.get(budgetId);\n    \n    if (!budget || !spending) {\n      throw new Error(`Budget ${budgetId} not found`);\n    }\n\n    const { amount, category = 'other', timestamp = new Date().toISOString() } = costData;\n    \n    // Update total spending\n    spending.totalSpending += amount;\n    spending.lastUpdated = timestamp;\n    \n    // Update category breakdown\n    if (spending.breakdown[category] !== undefined) {\n      spending.breakdown[category] += amount;\n    } else {\n      spending.breakdown.other += amount;\n    }\n    \n    // Update daily spending\n    const today = new Date().toISOString().split('T')[0];\n    let todaySpending = spending.dailySpending.find(d => d.date === today);\n    \n    if (!todaySpending) {\n      todaySpending = { date: today, amount: 0, breakdown: { compute: 0, storage: 0, network: 0, other: 0 } };\n      spending.dailySpending.push(todaySpending);\n      \n      // Keep only last 90 days\n      if (spending.dailySpending.length > 90) {\n        spending.dailySpending.shift();\n      }\n    }\n    \n    todaySpending.amount += amount;\n    todaySpending.breakdown[category] = (todaySpending.breakdown[category] || 0) + amount;\n    \n    // Update budget current spending and utilization\n    budget.currentSpending = spending.totalSpending;\n    budget.utilizationPercentage = (spending.totalSpending / budget.amount) * 100;\n    budget.projectedSpending = this.calculateProjectedSpending(budget, spending);\n    \n    // Check for alert thresholds\n    this.checkBudgetAlerts(budget);\n    \n    this.emit('budget_updated', { budgetId, budget, spending });\n    \n    return { budget, spending };\n  }\n\n  calculateProjectedSpending(budget, spending) {\n    if (spending.dailySpending.length < 7) {\n      return budget.currentSpending; // Not enough data for projection\n    }\n    \n    const recentDays = spending.dailySpending.slice(-14); // Last 14 days\n    const avgDailySpending = recentDays.reduce((sum, day) => sum + day.amount, 0) / recentDays.length;\n    \n    const budgetStart = new Date(budget.startDate);\n    const budgetEnd = new Date(budget.endDate);\n    const totalDays = Math.ceil((budgetEnd - budgetStart) / (1000 * 60 * 60 * 24));\n    const elapsedDays = Math.ceil((Date.now() - budgetStart) / (1000 * 60 * 60 * 24));\n    const remainingDays = Math.max(0, totalDays - elapsedDays);\n    \n    return budget.currentSpending + (avgDailySpending * remainingDays);\n  }\n\n  checkBudgetAlerts(budget) {\n    const activeAlerts = budget.alerts.filter(alert => alert.enabled);\n    \n    activeAlerts.forEach(alert => {\n      const thresholdAmount = budget.amount * alert.threshold;\n      const projectedThresholdAmount = budget.amount * alert.threshold;\n      \n      // Check current spending against threshold\n      if (budget.currentSpending >= thresholdAmount) {\n        this.triggerAlert(budget, alert, 'current', {\n          currentSpending: budget.currentSpending,\n          thresholdAmount,\n          overageAmount: budget.currentSpending - thresholdAmount\n        });\n      }\n      \n      // Check projected spending against threshold\n      if (budget.projectedSpending >= projectedThresholdAmount && \n          budget.currentSpending < thresholdAmount) {\n        this.triggerAlert(budget, alert, 'projected', {\n          projectedSpending: budget.projectedSpending,\n          thresholdAmount: projectedThresholdAmount,\n          daysUntilExceeded: this.calculateDaysUntilExceeded(budget, thresholdAmount)\n        });\n      }\n    });\n  }\n\n  calculateDaysUntilExceeded(budget, thresholdAmount) {\n    const spending = this.spending.get(budget.id);\n    if (!spending || spending.dailySpending.length < 3) return null;\n    \n    const recentDays = spending.dailySpending.slice(-7);\n    const avgDailySpending = recentDays.reduce((sum, day) => sum + day.amount, 0) / recentDays.length;\n    \n    if (avgDailySpending <= 0) return null;\n    \n    const remainingAmount = thresholdAmount - budget.currentSpending;\n    return Math.ceil(remainingAmount / avgDailySpending);\n  }\n\n  triggerAlert(budget, alertConfig, triggerType, alertData) {\n    const alertId = `alert_${budget.id}_${alertConfig.threshold}_${Date.now()}`;\n    \n    // Check if we've already sent this alert recently (avoid spam)\n    const recentAlertKey = `${budget.id}_${alertConfig.threshold}_${triggerType}`;\n    const lastAlert = this.notifications.get(recentAlertKey);\n    \n    if (lastAlert && Date.now() - lastAlert.timestamp < 3600000) { // 1 hour cooldown\n      return;\n    }\n    \n    const alert = {\n      id: alertId,\n      budgetId: budget.id,\n      budgetName: budget.name,\n      type: alertConfig.type,\n      threshold: alertConfig.threshold,\n      triggerType, // 'current' or 'projected'\n      severity: this.getAlertSeverity(alertConfig.threshold),\n      message: this.generateAlertMessage(budget, alertConfig, triggerType, alertData),\n      data: alertData,\n      timestamp: new Date().toISOString(),\n      acknowledged: false,\n      actions: this.determineAutomatedActions(budget, alertConfig)\n    };\n    \n    this.alerts.set(alertId, alert);\n    this.notifications.set(recentAlertKey, { timestamp: Date.now(), alertId });\n    \n    // Execute automated actions\n    this.executeAutomatedActions(alert);\n    \n    // Emit alert event\n    this.emit('budget_alert', alert);\n    \n    // Send notifications\n    this.sendNotifications(alert);\n    \n    return alert;\n  }\n\n  generateAlertMessage(budget, alertConfig, triggerType, alertData) {\n    const percentage = Math.round(alertConfig.threshold * 100);\n    const currentPercentage = Math.round(budget.utilizationPercentage);\n    \n    let message = `Budget \"${budget.name}\" has `;\n    \n    if (triggerType === 'current') {\n      if (alertConfig.threshold >= 1.0) {\n        message += `exceeded its limit by $${alertData.overageAmount.toFixed(2)} (${currentPercentage}% used)`;\n      } else {\n        message += `reached ${percentage}% of its limit ($${alertData.currentSpending.toFixed(2)} of $${budget.amount})`;\n      }\n    } else {\n      message += `projected to reach ${percentage}% of its limit`;\n      if (alertData.daysUntilExceeded) {\n        message += ` in approximately ${alertData.daysUntilExceeded} days`;\n      }\n      message += ` (projected: $${alertData.projectedSpending.toFixed(2)})`;\n    }\n    \n    return message;\n  }\n\n  getAlertSeverity(threshold) {\n    if (threshold >= 1.0) return 'critical';\n    if (threshold >= 0.9) return 'high';\n    if (threshold >= 0.8) return 'medium';\n    return 'low';\n  }\n\n  determineAutomatedActions(budget, alertConfig) {\n    const actions = [];\n    \n    // Get configured actions for this budget\n    const budgetActions = budget.actions.filter(action => \n      action.threshold <= alertConfig.threshold\n    );\n    \n    budgetActions.forEach(action => {\n      switch (action.type) {\n        case 'email_notification':\n          actions.push({ type: 'email', recipients: action.recipients });\n          break;\n        case 'disable_resources':\n          if (alertConfig.threshold >= 1.0) { // Only for exceeded budgets\n            actions.push({ type: 'disable_resources', resources: action.resources });\n          }\n          break;\n        case 'scale_down':\n          actions.push({ type: 'scale_down', resources: action.resources, factor: action.factor });\n          break;\n        case 'webhook':\n          actions.push({ type: 'webhook', url: action.url, payload: action.payload });\n          break;\n      }\n    });\n    \n    return actions;\n  }\n\n  async executeAutomatedActions(alert) {\n    for (const action of alert.actions) {\n      try {\n        await this.executeAction(action, alert);\n      } catch (error) {\n        console.error(`Failed to execute action ${action.type}: ${error.message}`);\n        this.emit('action_failed', { alert, action, error: error.message });\n      }\n    }\n  }\n\n  async executeAction(action, alert) {\n    switch (action.type) {\n      case 'email':\n        await this.sendEmailNotification(alert, action.recipients);\n        break;\n      case 'disable_resources':\n        await this.disableResources(action.resources, alert);\n        break;\n      case 'scale_down':\n        await this.scaleDownResources(action.resources, action.factor, alert);\n        break;\n      case 'webhook':\n        await this.sendWebhook(action.url, action.payload, alert);\n        break;\n      default:\n        console.warn(`Unknown action type: ${action.type}`);\n    }\n  }\n\n  async sendEmailNotification(alert, recipients) {\n    console.log(`[EMAIL] Sending budget alert to ${recipients.join(', ')}`);\n    console.log(`Subject: Budget Alert - ${alert.budgetName}`);\n    console.log(`Message: ${alert.message}`);\n    \n    this.emit('email_sent', { alert, recipients });\n  }\n\n  async disableResources(resources, alert) {\n    console.log(`[ACTION] Disabling resources due to budget exceeded:`, resources);\n    \n    this.emit('resources_disabled', { alert, resources });\n  }\n\n  async scaleDownResources(resources, factor, alert) {\n    console.log(`[ACTION] Scaling down resources by ${factor}x:`, resources);\n    \n    this.emit('resources_scaled', { alert, resources, factor });\n  }\n\n  async sendWebhook(url, payload, alert) {\n    console.log(`[WEBHOOK] Sending to ${url}:`, { ...payload, alert });\n    \n    this.emit('webhook_sent', { alert, url, payload });\n  }\n\n  sendNotifications(alert) {\n    // This would integrate with notification systems\n    console.log(`[NOTIFICATION] ${alert.severity.toUpperCase()}: ${alert.message}`);\n  }\n\n  startBudgetMonitoring() {\n    if (this.budgetCheckTimer) {\n      clearInterval(this.budgetCheckTimer);\n    }\n    \n    this.budgetCheckTimer = setInterval(() => {\n      this.checkAllBudgets();\n    }, this.checkInterval);\n  }\n\n  stopBudgetMonitoring() {\n    if (this.budgetCheckTimer) {\n      clearInterval(this.budgetCheckTimer);\n      this.budgetCheckTimer = null;\n    }\n  }\n\n  checkAllBudgets() {\n    for (const [budgetId, budget] of this.budgets) {\n      if (budget.status === 'active') {\n        this.checkBudgetAlerts(budget);\n        this.checkBudgetExpiry(budget);\n      }\n    }\n  }\n\n  checkBudgetExpiry(budget) {\n    const endDate = new Date(budget.endDate);\n    const now = new Date();\n    \n    if (now > endDate && budget.status === 'active') {\n      budget.status = 'expired';\n      \n      this.emit('budget_expired', {\n        budgetId: budget.id,\n        budget,\n        finalSpending: budget.currentSpending,\n        utilizationPercentage: budget.utilizationPercentage\n      });\n      \n      // Optionally create a new budget for the next period\n      if (budget.autoRenew) {\n        this.renewBudget(budget.id);\n      }\n    }\n  }\n\n  renewBudget(budgetId) {\n    const originalBudget = this.budgets.get(budgetId);\n    if (!originalBudget) return;\n    \n    const newBudget = {\n      ...originalBudget,\n      id: `budget_${Date.now()}_${Math.random().toString(36).substr(2, 9)}`,\n      startDate: originalBudget.endDate,\n      endDate: this.calculateEndDate(originalBudget.endDate, originalBudget.period),\n      currentSpending: 0,\n      projectedSpending: 0,\n      utilizationPercentage: 0,\n      createdAt: new Date().toISOString(),\n      status: 'active'\n    };\n    \n    this.budgets.set(newBudget.id, newBudget);\n    this.initializeBudgetTracking(newBudget);\n    \n    this.emit('budget_renewed', { originalBudgetId: budgetId, newBudget });\n    \n    return newBudget;\n  }\n\n  getBudget(budgetId) {\n    return this.budgets.get(budgetId);\n  }\n\n  getBudgetSpending(budgetId) {\n    return this.spending.get(budgetId);\n  }\n\n  getAllBudgets() {\n    return Array.from(this.budgets.values());\n  }\n\n  getBudgetsByScope(scope, scopeId) {\n    return Array.from(this.budgets.values()).filter(budget => \n      budget.scope === scope && \n      (budget.scopeIds.length === 0 || budget.scopeIds.includes(scopeId))\n    );\n  }\n\n  getActiveAlerts() {\n    return Array.from(this.alerts.values()).filter(alert => !alert.acknowledged);\n  }\n\n  acknowledgeAlert(alertId) {\n    const alert = this.alerts.get(alertId);\n    if (alert) {\n      alert.acknowledged = true;\n      alert.acknowledgedAt = new Date().toISOString();\n      this.emit('alert_acknowledged', alert);\n    }\n    return alert;\n  }\n\n  updateBudget(budgetId, updates) {\n    const budget = this.budgets.get(budgetId);\n    if (!budget) {\n      throw new Error(`Budget ${budgetId} not found`);\n    }\n    \n    // Update allowed fields\n    const allowedFields = ['name', 'amount', 'alerts', 'actions', 'tags'];\n    allowedFields.forEach(field => {\n      if (updates[field] !== undefined) {\n        budget[field] = updates[field];\n      }\n    });\n    \n    budget.updatedAt = new Date().toISOString();\n    \n    // Recalculate utilization if amount changed\n    if (updates.amount) {\n      budget.utilizationPercentage = (budget.currentSpending / budget.amount) * 100;\n    }\n    \n    this.emit('budget_updated', { budgetId, budget });\n    \n    return budget;\n  }\n\n  deleteBudget(budgetId) {\n    const budget = this.budgets.get(budgetId);\n    if (!budget) {\n      throw new Error(`Budget ${budgetId} not found`);\n    }\n    \n    this.budgets.delete(budgetId);\n    this.spending.delete(budgetId);\n    \n    // Remove related alerts\n    const alertsToRemove = Array.from(this.alerts.keys()).filter(alertId => \n      this.alerts.get(alertId).budgetId === budgetId\n    );\n    \n    alertsToRemove.forEach(alertId => this.alerts.delete(alertId));\n    \n    this.emit('budget_deleted', { budgetId, budget });\n    \n    return true;\n  }\n\n  generateBudgetReport(budgetId, options = {}) {\n    const budget = this.budgets.get(budgetId);\n    const spending = this.spending.get(budgetId);\n    \n    if (!budget || !spending) {\n      throw new Error(`Budget ${budgetId} not found`);\n    }\n    \n    const { includeDetails = true, includeForecast = true } = options;\n    \n    const report = {\n      budget: {\n        id: budget.id,\n        name: budget.name,\n        amount: budget.amount,\n        currency: budget.currency,\n        period: budget.period,\n        startDate: budget.startDate,\n        endDate: budget.endDate,\n        status: budget.status\n      },\n      spending: {\n        current: budget.currentSpending,\n        projected: budget.projectedSpending,\n        utilization: budget.utilizationPercentage,\n        remaining: Math.max(0, budget.amount - budget.currentSpending),\n        breakdown: spending.breakdown\n      },\n      timeline: this.generateSpendingTimeline(spending),\n      alerts: this.getAlertsForBudget(budgetId),\n      generatedAt: new Date().toISOString()\n    };\n    \n    if (includeDetails) {\n      report.dailySpending = spending.dailySpending;\n    }\n    \n    if (includeForecast) {\n      report.forecast = this.generateSpendingForecast(budget, spending);\n    }\n    \n    return report;\n  }\n\n  generateSpendingTimeline(spending) {\n    const days = spending.dailySpending.slice(-30); // Last 30 days\n    let cumulativeSpending = 0;\n    \n    return days.map(day => {\n      cumulativeSpending += day.amount;\n      return {\n        date: day.date,\n        dailySpending: day.amount,\n        cumulativeSpending: Math.round(cumulativeSpending * 100) / 100,\n        breakdown: day.breakdown\n      };\n    });\n  }\n\n  generateSpendingForecast(budget, spending) {\n    const recentDays = spending.dailySpending.slice(-14);\n    if (recentDays.length < 7) {\n      return { error: 'Insufficient data for forecasting' };\n    }\n    \n    const avgDailySpending = recentDays.reduce((sum, day) => sum + day.amount, 0) / recentDays.length;\n    const endDate = new Date(budget.endDate);\n    const remainingDays = Math.max(0, Math.ceil((endDate - Date.now()) / (1000 * 60 * 60 * 24)));\n    \n    const forecastedTotal = budget.currentSpending + (avgDailySpending * remainingDays);\n    const willExceed = forecastedTotal > budget.amount;\n    \n    return {\n      avgDailySpending: Math.round(avgDailySpending * 100) / 100,\n      remainingDays,\n      forecastedTotal: Math.round(forecastedTotal * 100) / 100,\n      forecastedOverage: willExceed ? Math.round((forecastedTotal - budget.amount) * 100) / 100 : 0,\n      willExceedBudget: willExceed,\n      confidence: Math.min(1, recentDays.length / 14) // Based on amount of historical data\n    };\n  }\n\n  getAlertsForBudget(budgetId) {\n    return Array.from(this.alerts.values()).filter(alert => alert.budgetId === budgetId);\n  }\n\n  exportBudgetData(format = 'json') {\n    const data = {\n      timestamp: new Date().toISOString(),\n      budgets: Array.from(this.budgets.values()),\n      spending: Object.fromEntries(this.spending),\n      alerts: Array.from(this.alerts.values()),\n      summary: {\n        totalBudgets: this.budgets.size,\n        activeBudgets: Array.from(this.budgets.values()).filter(b => b.status === 'active').length,\n        totalAllocated: Array.from(this.budgets.values()).reduce((sum, b) => sum + b.amount, 0),\n        totalSpent: Array.from(this.budgets.values()).reduce((sum, b) => sum + b.currentSpending, 0)\n      }\n    };\n    \n    if (format === 'json') {\n      return JSON.stringify(data, null, 2);\n    }\n    \n    return data;\n  }\n}\n\nmodule.exports = BudgetManager;