const fs = require('fs').promises;
const path = require('path');
const crypto = require('crypto');
const archiver = require('archiver');
const { EventEmitter } = require('events');

class FirmwareOTA extends EventEmitter {
  constructor(options = {}) {
    super();
    this.firmwareStore = options.firmwareStore || path.join(__dirname, '../../firmware');
    this.packages = new Map();
    this.deployments = new Map();
    this.deviceUpdates = new Map();
    this.rollouts = new Map();
    
    this.options = {\n      maxPackageSize: options.maxPackageSize || 50 * 1024 * 1024, // 50MB\n      supportedFormats: options.supportedFormats || ['.bin', '.hex', '.elf', '.zip'],\n      checksumAlgorithm: options.checksumAlgorithm || 'sha256',\n      enableRollback: options.enableRollback !== false,\n      maxRolloutBatchSize: options.maxRolloutBatchSize || 100,\n      rolloutBatchDelay: options.rolloutBatchDelay || 300000, // 5 minutes\n      ...options\n    };\n    \n    this.updateStates = {\n      PENDING: 'pending',\n      DOWNLOADING: 'downloading',\n      DOWNLOADED: 'downloaded',\n      INSTALLING: 'installing',\n      INSTALLED: 'installed',\n      FAILED: 'failed',\n      ROLLED_BACK: 'rolled_back'\n    };\n    \n    this.rolloutStrategies = {\n      IMMEDIATE: 'immediate',\n      GRADUAL: 'gradual',\n      SCHEDULED: 'scheduled',\n      CANARY: 'canary'\n    };\n    \n    this.initializeStorage();\n  }\n\n  async initializeStorage() {\n    try {\n      await fs.mkdir(this.firmwareStore, { recursive: true });\n      await fs.mkdir(path.join(this.firmwareStore, 'packages'), { recursive: true });\n      await fs.mkdir(path.join(this.firmwareStore, 'temp'), { recursive: true });\n      console.log('📁 Firmware storage initialized');\n    } catch (error) {\n      console.error('❌ Failed to initialize firmware storage:', error);\n    }\n  }\n\n  // Firmware package management\n  async createFirmwarePackage(packageData) {\n    const packageId = this.generatePackageId();\n    const {\n      name,\n      version,\n      deviceType,\n      description,\n      filePath,\n      metadata = {},\n      releaseNotes = '',\n      minVersion = null,\n      maxVersion = null\n    } = packageData;\n\n    // Validate file\n    const fileStats = await fs.stat(filePath);\n    if (fileStats.size > this.options.maxPackageSize) {\n      throw new Error(`Package size exceeds maximum allowed size of ${this.options.maxPackageSize} bytes`);\n    }\n\n    const fileExtension = path.extname(filePath);\n    if (!this.options.supportedFormats.includes(fileExtension)) {\n      throw new Error(`Unsupported file format: ${fileExtension}`);\n    }\n\n    // Calculate checksum\n    const fileBuffer = await fs.readFile(filePath);\n    const checksum = crypto.createHash(this.options.checksumAlgorithm)\n      .update(fileBuffer)\n      .digest('hex');\n\n    // Store firmware file\n    const storedFileName = `${packageId}_${version}${fileExtension}`;\n    const storedFilePath = path.join(this.firmwareStore, 'packages', storedFileName);\n    await fs.copyFile(filePath, storedFilePath);\n\n    const firmwarePackage = {\n      id: packageId,\n      name,\n      version,\n      deviceType,\n      description,\n      releaseNotes,\n      filePath: storedFilePath,\n      fileName: storedFileName,\n      fileSize: fileStats.size,\n      checksum,\n      checksumAlgorithm: this.options.checksumAlgorithm,\n      minVersion,\n      maxVersion,\n      metadata,\n      createdAt: new Date().toISOString(),\n      status: 'active',\n      downloadCount: 0,\n      successfulInstalls: 0,\n      failedInstalls: 0\n    };\n\n    this.packages.set(packageId, firmwarePackage);\n    console.log(`📦 Firmware package created: ${name} v${version} (${packageId})`);\n    \n    this.emit('package_created', firmwarePackage);\n    return firmwarePackage;\n  }\n\n  getFirmwarePackage(packageId) {\n    return this.packages.get(packageId);\n  }\n\n  getAllFirmwarePackages() {\n    return Array.from(this.packages.values());\n  }\n\n  getFirmwarePackagesByDeviceType(deviceType) {\n    return Array.from(this.packages.values())\n      .filter(pkg => pkg.deviceType === deviceType && pkg.status === 'active')\n      .sort((a, b) => new Date(b.createdAt) - new Date(a.createdAt));\n  }\n\n  async deleteFirmwarePackage(packageId) {\n    const pkg = this.packages.get(packageId);\n    if (!pkg) {\n      throw new Error(`Firmware package ${packageId} not found`);\n    }\n\n    // Check if package is being used in active deployments\n    const activeDeployments = Array.from(this.deployments.values())\n      .filter(d => d.packageId === packageId && ['pending', 'in_progress'].includes(d.status));\n    \n    if (activeDeployments.length > 0) {\n      throw new Error(`Cannot delete package ${packageId}: has active deployments`);\n    }\n\n    // Delete file\n    try {\n      await fs.unlink(pkg.filePath);\n    } catch (error) {\n      console.warn(`Warning: Could not delete firmware file ${pkg.filePath}:`, error.message);\n    }\n\n    // Remove from packages\n    this.packages.delete(packageId);\n    pkg.status = 'deleted';\n    pkg.deletedAt = new Date().toISOString();\n\n    this.emit('package_deleted', pkg);\n    return pkg;\n  }\n\n  // Deployment management\n  createDeployment(deploymentConfig) {\n    const deploymentId = this.generateDeploymentId();\n    const {\n      name,\n      packageId,\n      targetDevices = [],\n      targetGroups = [],\n      rolloutStrategy = this.rolloutStrategies.IMMEDIATE,\n      rolloutConfig = {},\n      scheduledAt = null,\n      description = '',\n      createdBy = 'system'\n    } = deploymentConfig;\n\n    // Validate package exists\n    const pkg = this.packages.get(packageId);\n    if (!pkg) {\n      throw new Error(`Firmware package ${packageId} not found`);\n    }\n\n    if (pkg.status !== 'active') {\n      throw new Error(`Firmware package ${packageId} is not active`);\n    }\n\n    const deployment = {\n      id: deploymentId,\n      name,\n      packageId,\n      targetDevices: [...targetDevices],\n      targetGroups: [...targetGroups],\n      rolloutStrategy,\n      rolloutConfig: {\n        batchSize: rolloutConfig.batchSize || this.options.maxRolloutBatchSize,\n        batchDelay: rolloutConfig.batchDelay || this.options.rolloutBatchDelay,\n        successThreshold: rolloutConfig.successThreshold || 0.95,\n        canaryPercentage: rolloutConfig.canaryPercentage || 0.1,\n        ...rolloutConfig\n      },\n      scheduledAt,\n      description,\n      createdBy,\n      createdAt: new Date().toISOString(),\n      status: scheduledAt ? 'scheduled' : 'pending',\n      deviceUpdates: [],\n      stats: {\n        totalDevices: targetDevices.length,\n        pending: 0,\n        downloading: 0,\n        downloaded: 0,\n        installing: 0,\n        installed: 0,\n        failed: 0,\n        rolledBack: 0\n      }\n    };\n\n    this.deployments.set(deploymentId, deployment);\n    console.log(`🚀 Deployment created: ${name} (${deploymentId})`);\n\n    // Start deployment if not scheduled\n    if (!scheduledAt) {\n      setTimeout(() => this.startDeployment(deploymentId), 1000);\n    }\n\n    this.emit('deployment_created', deployment);\n    return deployment;\n  }\n\n  async startDeployment(deploymentId) {\n    const deployment = this.deployments.get(deploymentId);\n    if (!deployment) {\n      throw new Error(`Deployment ${deploymentId} not found`);\n    }\n\n    if (deployment.status !== 'pending' && deployment.status !== 'scheduled') {\n      throw new Error(`Deployment ${deploymentId} cannot be started (status: ${deployment.status})`);\n    }\n\n    deployment.status = 'in_progress';\n    deployment.startedAt = new Date().toISOString();\n\n    console.log(`▶️ Starting deployment: ${deployment.name}`);\n\n    try {\n      await this.executeRolloutStrategy(deployment);\n      this.emit('deployment_started', deployment);\n    } catch (error) {\n      deployment.status = 'failed';\n      deployment.error = error.message;\n      deployment.failedAt = new Date().toISOString();\n      \n      console.error(`❌ Deployment failed: ${deployment.name}:`, error.message);\n      this.emit('deployment_failed', { deployment, error });\n      throw error;\n    }\n\n    return deployment;\n  }\n\n  async executeRolloutStrategy(deployment) {\n    const { rolloutStrategy } = deployment;\n\n    switch (rolloutStrategy) {\n      case this.rolloutStrategies.IMMEDIATE:\n        await this.executeImmediateRollout(deployment);\n        break;\n      case this.rolloutStrategies.GRADUAL:\n        await this.executeGradualRollout(deployment);\n        break;\n      case this.rolloutStrategies.CANARY:\n        await this.executeCanaryRollout(deployment);\n        break;\n      case this.rolloutStrategies.SCHEDULED:\n        await this.executeScheduledRollout(deployment);\n        break;\n      default:\n        throw new Error(`Unknown rollout strategy: ${rolloutStrategy}`);\n    }\n  }\n\n  async executeImmediateRollout(deployment) {\n    const rolloutId = this.generateRolloutId();\n    const rollout = {\n      id: rolloutId,\n      deploymentId: deployment.id,\n      strategy: 'immediate',\n      targetDevices: [...deployment.targetDevices],\n      status: 'active',\n      startedAt: new Date().toISOString()\n    };\n\n    this.rollouts.set(rolloutId, rollout);\n\n    // Send update to all target devices immediately\n    for (const deviceId of deployment.targetDevices) {\n      await this.sendUpdateToDevice(deployment, deviceId, rolloutId);\n    }\n\n    console.log(`⚡ Immediate rollout started for ${deployment.targetDevices.length} devices`);\n  }\n\n  async executeGradualRollout(deployment) {\n    const { batchSize, batchDelay } = deployment.rolloutConfig;\n    const rolloutId = this.generateRolloutId();\n    \n    const rollout = {\n      id: rolloutId,\n      deploymentId: deployment.id,\n      strategy: 'gradual',\n      targetDevices: [...deployment.targetDevices],\n      batchSize,\n      batchDelay,\n      currentBatch: 0,\n      status: 'active',\n      startedAt: new Date().toISOString()\n    };\n\n    this.rollouts.set(rolloutId, rollout);\n\n    const devices = deployment.targetDevices;\n    const batches = [];\n    \n    for (let i = 0; i < devices.length; i += batchSize) {\n      batches.push(devices.slice(i, i + batchSize));\n    }\n\n    console.log(`📊 Gradual rollout: ${batches.length} batches of ${batchSize} devices`);\n\n    for (let batchIndex = 0; batchIndex < batches.length; batchIndex++) {\n      const batch = batches[batchIndex];\n      rollout.currentBatch = batchIndex + 1;\n      \n      console.log(`📦 Processing batch ${batchIndex + 1}/${batches.length} (${batch.length} devices)`);\n      \n      // Send updates to batch\n      for (const deviceId of batch) {\n        await this.sendUpdateToDevice(deployment, deviceId, rolloutId);\n      }\n      \n      // Wait before next batch (except for last batch)\n      if (batchIndex < batches.length - 1) {\n        await this.delay(batchDelay);\n      }\n    }\n\n    rollout.status = 'completed';\n    rollout.completedAt = new Date().toISOString();\n  }\n\n  async executeCanaryRollout(deployment) {\n    const { canaryPercentage, successThreshold } = deployment.rolloutConfig;\n    const rolloutId = this.generateRolloutId();\n    \n    const totalDevices = deployment.targetDevices.length;\n    const canarySize = Math.max(1, Math.floor(totalDevices * canaryPercentage));\n    const canaryDevices = deployment.targetDevices.slice(0, canarySize);\n    const remainingDevices = deployment.targetDevices.slice(canarySize);\n\n    const rollout = {\n      id: rolloutId,\n      deploymentId: deployment.id,\n      strategy: 'canary',\n      canaryDevices,\n      remainingDevices,\n      canarySize,\n      successThreshold,\n      phase: 'canary',\n      status: 'active',\n      startedAt: new Date().toISOString()\n    };\n\n    this.rollouts.set(rolloutId, rollout);\n\n    console.log(`🐦 Canary rollout: ${canarySize} canary devices, ${remainingDevices.length} remaining`);\n\n    // Phase 1: Canary deployment\n    for (const deviceId of canaryDevices) {\n      await this.sendUpdateToDevice(deployment, deviceId, rolloutId);\n    }\n\n    // Wait for canary results and evaluate success\n    setTimeout(() => {\n      this.evaluateCanaryResults(rolloutId);\n    }, 600000); // 10 minutes\n  }\n\n  async evaluateCanaryResults(rolloutId) {\n    const rollout = this.rollouts.get(rolloutId);\n    const deployment = this.deployments.get(rollout.deploymentId);\n    \n    if (!rollout || !deployment) return;\n\n    const canaryUpdates = rollout.canaryDevices.map(deviceId => \n      this.deviceUpdates.get(`${deployment.id}_${deviceId}`)\n    ).filter(Boolean);\n\n    const successCount = canaryUpdates.filter(update => \n      update.status === this.updateStates.INSTALLED\n    ).length;\n    \n    const successRate = canaryUpdates.length > 0 ? successCount / canaryUpdates.length : 0;\n\n    console.log(`📊 Canary evaluation: ${successCount}/${canaryUpdates.length} successful (${Math.round(successRate * 100)}%)`);\n\n    if (successRate >= rollout.successThreshold) {\n      // Proceed with full rollout\n      rollout.phase = 'full_rollout';\n      console.log(`✅ Canary successful, proceeding with full rollout`);\n      \n      for (const deviceId of rollout.remainingDevices) {\n        await this.sendUpdateToDevice(deployment, deviceId, rolloutId);\n      }\n      \n      rollout.status = 'completed';\n      rollout.completedAt = new Date().toISOString();\n    } else {\n      // Rollback canary devices\n      rollout.phase = 'rollback';\n      rollout.status = 'failed';\n      console.log(`❌ Canary failed, rolling back`);\n      \n      for (const deviceId of rollout.canaryDevices) {\n        await this.rollbackDevice(deviceId, 'Canary rollout failed');\n      }\n    }\n\n    this.emit('canary_evaluated', { rollout, deployment, successRate });\n  }\n\n  async sendUpdateToDevice(deployment, deviceId, rolloutId) {\n    const updateId = `${deployment.id}_${deviceId}`;\n    const pkg = this.packages.get(deployment.packageId);\n    \n    if (!pkg) {\n      throw new Error(`Package ${deployment.packageId} not found`);\n    }\n\n    const deviceUpdate = {\n      id: updateId,\n      deploymentId: deployment.id,\n      rolloutId,\n      deviceId,\n      packageId: deployment.packageId,\n      packageVersion: pkg.version,\n      status: this.updateStates.PENDING,\n      downloadUrl: this.generateDownloadUrl(deployment.packageId),\n      checksum: pkg.checksum,\n      checksumAlgorithm: pkg.checksumAlgorithm,\n      fileSize: pkg.fileSize,\n      createdAt: new Date().toISOString(),\n      retryCount: 0,\n      maxRetries: 3\n    };\n\n    this.deviceUpdates.set(updateId, deviceUpdate);\n    deployment.deviceUpdates.push(updateId);\n    deployment.stats.pending++;\n    deployment.stats.totalDevices = Math.max(deployment.stats.totalDevices, deployment.deviceUpdates.length);\n\n    // Publish update notification via MQTT\n    this.publishUpdateNotification(deviceUpdate);\n\n    console.log(`📱 Update sent to device ${deviceId}: ${pkg.name} v${pkg.version}`);\n    this.emit('update_sent', deviceUpdate);\n    \n    return deviceUpdate;\n  }\n\n  publishUpdateNotification(deviceUpdate) {\n    const topic = `$system/device/${deviceUpdate.deviceId}/ota`;\n    const payload = {\n      updateId: deviceUpdate.id,\n      packageId: deviceUpdate.packageId,\n      version: deviceUpdate.packageVersion,\n      downloadUrl: deviceUpdate.downloadUrl,\n      checksum: deviceUpdate.checksum,\n      checksumAlgorithm: deviceUpdate.checksumAlgorithm,\n      fileSize: deviceUpdate.fileSize,\n      timestamp: new Date().toISOString()\n    };\n\n    this.emit('mqtt_publish', { topic, payload });\n  }\n\n  // Device update status handling\n  updateDeviceStatus(deviceId, deploymentId, status, metadata = {}) {\n    const updateId = `${deploymentId}_${deviceId}`;\n    const deviceUpdate = this.deviceUpdates.get(updateId);\n    const deployment = this.deployments.get(deploymentId);\n    \n    if (!deviceUpdate || !deployment) {\n      console.warn(`Update or deployment not found: ${updateId}`);\n      return;\n    }\n\n    const previousStatus = deviceUpdate.status;\n    deviceUpdate.status = status;\n    deviceUpdate.lastUpdated = new Date().toISOString();\n    deviceUpdate.metadata = { ...deviceUpdate.metadata, ...metadata };\n\n    // Update deployment stats\n    if (previousStatus !== status) {\n      deployment.stats[previousStatus]--;\n      deployment.stats[status]++;\n    }\n\n    // Handle specific status changes\n    switch (status) {\n      case this.updateStates.DOWNLOADING:\n        deviceUpdate.downloadStartedAt = new Date().toISOString();\n        break;\n      case this.updateStates.DOWNLOADED:\n        deviceUpdate.downloadCompletedAt = new Date().toISOString();\n        this.packages.get(deviceUpdate.packageId).downloadCount++;\n        break;\n      case this.updateStates.INSTALLING:\n        deviceUpdate.installStartedAt = new Date().toISOString();\n        break;\n      case this.updateStates.INSTALLED:\n        deviceUpdate.installCompletedAt = new Date().toISOString();\n        this.packages.get(deviceUpdate.packageId).successfulInstalls++;\n        break;\n      case this.updateStates.FAILED:\n        deviceUpdate.failedAt = new Date().toISOString();\n        deviceUpdate.retryCount++;\n        this.packages.get(deviceUpdate.packageId).failedInstalls++;\n        \n        // Retry logic\n        if (deviceUpdate.retryCount < deviceUpdate.maxRetries) {\n          setTimeout(() => {\n            this.retryDeviceUpdate(updateId);\n          }, 60000 * deviceUpdate.retryCount); // Exponential backoff\n        }\n        break;\n    }\n\n    // Check if deployment is complete\n    if (this.isDeploymentComplete(deployment)) {\n      this.completeDeployment(deployment);\n    }\n\n    console.log(`📱 Device ${deviceId} update status: ${previousStatus} → ${status}`);\n    this.emit('device_update_status', { deviceUpdate, deployment, previousStatus });\n  }\n\n  retryDeviceUpdate(updateId) {\n    const deviceUpdate = this.deviceUpdates.get(updateId);\n    if (!deviceUpdate) return;\n\n    if (deviceUpdate.status === this.updateStates.FAILED && \n        deviceUpdate.retryCount < deviceUpdate.maxRetries) {\n      \n      deviceUpdate.status = this.updateStates.PENDING;\n      this.publishUpdateNotification(deviceUpdate);\n      \n      console.log(`🔄 Retrying update for device ${deviceUpdate.deviceId} (attempt ${deviceUpdate.retryCount + 1})`);\n      this.emit('update_retry', deviceUpdate);\n    }\n  }\n\n  isDeploymentComplete(deployment) {\n    const stats = deployment.stats;\n    const activeStates = stats.downloading + stats.installing + stats.pending;\n    return activeStates === 0;\n  }\n\n  completeDeployment(deployment) {\n    const stats = deployment.stats;\n    const successRate = stats.totalDevices > 0 ? stats.installed / stats.totalDevices : 0;\n    \n    deployment.status = successRate >= 0.8 ? 'completed' : 'partially_completed';\n    deployment.completedAt = new Date().toISOString();\n    deployment.successRate = successRate;\n\n    console.log(`✅ Deployment completed: ${deployment.name} (${Math.round(successRate * 100)}% success)`);\n    this.emit('deployment_completed', deployment);\n  }\n\n  // Rollback functionality\n  async rollbackDevice(deviceId, reason = 'Manual rollback') {\n    // Find the most recent successful update for this device\n    const deviceUpdates = Array.from(this.deviceUpdates.values())\n      .filter(update => \n        update.deviceId === deviceId && \n        update.status === this.updateStates.INSTALLED\n      )\n      .sort((a, b) => new Date(b.installCompletedAt) - new Date(a.installCompletedAt));\n\n    if (deviceUpdates.length < 2) {\n      throw new Error(`No previous firmware version found for rollback on device ${deviceId}`);\n    }\n\n    const previousUpdate = deviceUpdates[1]; // Second most recent\n    const rollbackId = this.generateUpdateId();\n    \n    const rollbackUpdate = {\n      id: rollbackId,\n      deviceId,\n      packageId: previousUpdate.packageId,\n      packageVersion: previousUpdate.packageVersion,\n      status: this.updateStates.PENDING,\n      isRollback: true,\n      rollbackReason: reason,\n      originalUpdateId: deviceUpdates[0].id,\n      downloadUrl: this.generateDownloadUrl(previousUpdate.packageId),\n      createdAt: new Date().toISOString()\n    };\n\n    this.deviceUpdates.set(rollbackId, rollbackUpdate);\n    this.publishUpdateNotification(rollbackUpdate);\n\n    console.log(`🔙 Rollback initiated for device ${deviceId}: ${reason}`);\n    this.emit('rollback_initiated', rollbackUpdate);\n    \n    return rollbackUpdate;\n  }\n\n  async rollbackDeployment(deploymentId, reason = 'Manual rollback') {\n    const deployment = this.deployments.get(deploymentId);\n    if (!deployment) {\n      throw new Error(`Deployment ${deploymentId} not found`);\n    }\n\n    const rollbackResults = [];\n    \n    for (const updateId of deployment.deviceUpdates) {\n      const deviceUpdate = this.deviceUpdates.get(updateId);\n      if (deviceUpdate && deviceUpdate.status === this.updateStates.INSTALLED) {\n        try {\n          const rollback = await this.rollbackDevice(deviceUpdate.deviceId, reason);\n          rollbackResults.push({ success: true, deviceId: deviceUpdate.deviceId, rollback });\n        } catch (error) {\n          rollbackResults.push({ success: false, deviceId: deviceUpdate.deviceId, error: error.message });\n        }\n      }\n    }\n\n    deployment.status = 'rolled_back';\n    deployment.rolledBackAt = new Date().toISOString();\n    deployment.rollbackReason = reason;\n\n    console.log(`🔙 Deployment rolled back: ${deployment.name}`);\n    this.emit('deployment_rolled_back', { deployment, rollbackResults });\n    \n    return { deployment, rollbackResults };\n  }\n\n  // Utility methods\n  generatePackageId() {\n    return `pkg_${Date.now()}_${crypto.randomBytes(8).toString('hex')}`;\n  }\n\n  generateDeploymentId() {\n    return `dep_${Date.now()}_${crypto.randomBytes(8).toString('hex')}`;\n  }\n\n  generateRolloutId() {\n    return `roll_${Date.now()}_${crypto.randomBytes(6).toString('hex')}`;\n  }\n\n  generateUpdateId() {\n    return `upd_${Date.now()}_${crypto.randomBytes(6).toString('hex')}`;\n  }\n\n  generateDownloadUrl(packageId) {\n    return `/api/v1/iot/firmware/download/${packageId}`;\n  }\n\n  delay(ms) {\n    return new Promise(resolve => setTimeout(resolve, ms));\n  }\n\n  // Statistics and reporting\n  getOTAStats() {\n    const packages = Array.from(this.packages.values());\n    const deployments = Array.from(this.deployments.values());\n    const updates = Array.from(this.deviceUpdates.values());\n\n    return {\n      packages: {\n        total: packages.length,\n        active: packages.filter(p => p.status === 'active').length,\n        totalDownloads: packages.reduce((sum, p) => sum + p.downloadCount, 0),\n        totalSuccessfulInstalls: packages.reduce((sum, p) => sum + p.successfulInstalls, 0),\n        totalFailedInstalls: packages.reduce((sum, p) => sum + p.failedInstalls, 0)\n      },\n      deployments: {\n        total: deployments.length,\n        byStatus: this.groupBy(deployments, 'status'),\n        byStrategy: this.groupBy(deployments, 'rolloutStrategy')\n      },\n      updates: {\n        total: updates.length,\n        byStatus: this.groupBy(updates, 'status'),\n        rollbacks: updates.filter(u => u.isRollback).length\n      },\n      rollouts: {\n        total: this.rollouts.size,\n        byStrategy: this.groupBy(Array.from(this.rollouts.values()), 'strategy')\n      }\n    };\n  }\n\n  groupBy(array, key) {\n    return array.reduce((groups, item) => {\n      const value = item[key];\n      groups[value] = (groups[value] || 0) + 1;\n      return groups;\n    }, {});\n  }\n\n  getDeviceUpdateHistory(deviceId, limit = 50) {\n    return Array.from(this.deviceUpdates.values())\n      .filter(update => update.deviceId === deviceId)\n      .sort((a, b) => new Date(b.createdAt) - new Date(a.createdAt))\n      .slice(0, limit);\n  }\n\n  getDeploymentReport(deploymentId) {\n    const deployment = this.deployments.get(deploymentId);\n    if (!deployment) {\n      throw new Error(`Deployment ${deploymentId} not found`);\n    }\n\n    const updates = deployment.deviceUpdates.map(updateId => \n      this.deviceUpdates.get(updateId)\n    ).filter(Boolean);\n\n    const deviceSummary = updates.map(update => ({\n      deviceId: update.deviceId,\n      status: update.status,\n      startedAt: update.createdAt,\n      completedAt: update.installCompletedAt || update.failedAt,\n      retryCount: update.retryCount,\n      error: update.metadata?.error\n    }));\n\n    return {\n      deployment,\n      package: this.packages.get(deployment.packageId),\n      statistics: deployment.stats,\n      deviceSummary,\n      duration: deployment.completedAt ? \n        new Date(deployment.completedAt) - new Date(deployment.startedAt) : \n        Date.now() - new Date(deployment.startedAt || deployment.createdAt),\n      generatedAt: new Date().toISOString()\n    };\n  }\n\n  // File serving\n  async getPackageFile(packageId) {\n    const pkg = this.packages.get(packageId);\n    if (!pkg) {\n      throw new Error(`Package ${packageId} not found`);\n    }\n\n    try {\n      const fileBuffer = await fs.readFile(pkg.filePath);\n      return {\n        buffer: fileBuffer,\n        fileName: pkg.fileName,\n        contentType: this.getContentType(pkg.fileName),\n        size: pkg.fileSize\n      };\n    } catch (error) {\n      throw new Error(`Failed to read package file: ${error.message}`);\n    }\n  }\n\n  getContentType(fileName) {\n    const ext = path.extname(fileName).toLowerCase();\n    const types = {\n      '.bin': 'application/octet-stream',\n      '.hex': 'application/octet-stream',\n      '.elf': 'application/octet-stream',\n      '.zip': 'application/zip'\n    };\n    return types[ext] || 'application/octet-stream';\n  }\n\n  // Cleanup\n  async cleanupExpiredDeployments(daysOld = 30) {\n    const cutoffDate = new Date(Date.now() - daysOld * 24 * 60 * 60 * 1000);\n    let cleanupCount = 0;\n\n    for (const [deploymentId, deployment] of this.deployments) {\n      if (['completed', 'failed', 'rolled_back'].includes(deployment.status) &&\n          new Date(deployment.createdAt) < cutoffDate) {\n        \n        // Remove associated device updates\n        deployment.deviceUpdates.forEach(updateId => {\n          this.deviceUpdates.delete(updateId);\n        });\n        \n        this.deployments.delete(deploymentId);\n        cleanupCount++;\n      }\n    }\n\n    if (cleanupCount > 0) {\n      console.log(`🧹 Cleaned up ${cleanupCount} expired deployments`);\n    }\n\n    return cleanupCount;\n  }\n}\n\nmodule.exports = FirmwareOTA;