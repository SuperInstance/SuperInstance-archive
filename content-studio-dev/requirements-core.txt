# Core dependencies - install these first
# Lighter weight, essential packages only

# Core FastAPI and web server
fastapi==0.104.1
uvicorn[standard]==0.24.0
websockets==12.0
python-multipart==0.0.6

# Anthropic Claude API
anthropic==0.7.8

# Ollama for local LLMs
ollama==0.1.0

# Vector database (lighter, no torch dependency)
chromadb==0.4.18

# Database
sqlalchemy==2.0.23

# Data validation
pydantic==2.5.0

# File operations
aiofiles==23.2.1
watchfiles==0.21.0

# Configuration
pyyaml==6.0.1
python-dotenv==1.0.0

# Basic langchain (without all heavy dependencies)
langchain-core==0.1.0
langchain-community==0.0.1
